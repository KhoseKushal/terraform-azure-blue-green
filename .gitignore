# Terraform working directory
.terraform/

# Terraform state files (ALL variations)
*.tfstate
*.tfstate.*
*.backup

# Variable files (may contain secrets)
terraform.tfvars

# Terraform lock file
*.terraform.lock.hcl

# OS / Editor junk
.DS_Store
Thumbs.db
